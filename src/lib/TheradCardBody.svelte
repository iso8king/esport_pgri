<script>
    export let thread;
    export let timeAgo;
    export let onLike;
</script>

<div class="flex items-center gap-2.5 mb-2">
    {#if thread.author.pfp}
        <img src={thread.author.pfp} alt={thread.author.nama} class="w-8 h-8 rounded-full object-cover border border-gray-200" />
    {:else}
        <div class="w-8 h-8 rounded-full bg-gray-400 flex items-center justify-center">
            <span class="text-xs font-bold text-white">{thread.author.nama.charAt(0).toUpperCase()}</span>
        </div>
    {/if}
    <div>
        <p class="text-sm font-bold text-gray-800">{thread.author.nama}</p>
        <p class="text-xs text-gray-400">{timeAgo(thread.createdAt)}</p>
    </div>
</div>

<h3 class="font-bold text-[#0b355b] text-base mb-1">{thread.title}</h3>
<p class="text-sm text-gray-500 line-clamp-2 mb-3">{thread.content || "Baca Selengkap nya......"}</p>

<div class="flex items-center gap-4 pt-3 border-t border-gray-100">
    <button
        on:click|stopPropagation={onLike}
        class="flex items-center gap-1.5 text-xs font-semibold transition-colors {thread.likedByMe ? 'text-red-500' : 'text-gray-400 hover:text-red-400'}"
    >
        <svg class="w-4 h-4" fill={thread.likedByMe ? "currentColor" : "none"} viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
        </svg>
        {thread.likeCount}
    </button>
    <span class="flex items-center gap-1.5 text-xs text-gray-400">
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
        </svg>
        {thread.replyCount}
    </span>
</div>